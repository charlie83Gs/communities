apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# OpenFGA - Production
# Authorization service with production database connection
# Depends on: PostgreSQL cluster for OpenFGA, External Secrets for password management

resources:
  - ../../base
