# MANUAL SETUP REQUIRED
# This file documents secrets that must be created manually before deploying

# 1. GitHub Container Registry credentials (for pulling images)
# kubectl create secret docker-registry ghcr-credentials \
#   --docker-server=ghcr.io \
#   --docker-username=<GITHUB_USERNAME> \
#   --docker-password=<GITHUB_PAT> \
#   --namespace=communities-app

# 2. OpenFGA configuration
# kubectl create secret generic openfga-config \
#   --from-literal=store-id=<OPENFGA_STORE_ID> \
#   --namespace=communities-app

# Note: The postgres-api-app secret is created automatically by CNPG
