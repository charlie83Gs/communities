apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# CNPG - Production
# CloudNativePG operator with production-specific configuration

resources:
  - ../../base

patches:
  - path: helmrelease-patch.yaml
    target:
      kind: HelmRelease
      name: cnpg
