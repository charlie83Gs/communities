apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Traefik - Production
# High-availability ingress controller configuration
# Depends on: DigitalOcean LoadBalancer

namespace: traefik

resources:
  - ../../base

patches:
  - path: helmrelease-patch.yaml
    target:
      kind: HelmRelease
      name: traefik
